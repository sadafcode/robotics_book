{
  "version": 2,
  "buildCommand": "cd auth-service && npm install",
  "installCommand": "cd auth-service && npm install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/auth-service"
    }
  ]
}
